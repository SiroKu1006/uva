#include<iostream>
#include<vector>
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int t,n,p,w,g;
    cin >> t ;
    while (t--)
    {
        cin >> n ;
        vector<int> p_li(n,0);
        vector<int> w_li(n,0);
        while (n--)
        {
            cin >> p >> w;
            p_li.push_back(p);
            k_li.push_back(k);
            
        }
        
    }
    
    system("pause");
}