#include<iostream>
#include<vector>
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    char a,b;
    while (cin >> s >> j)
    {
        vector<int> a(26,0);
        vector<int> b(26,0);
        for(auto& i:a){
            ++a[]
        }
    }
    
    system("pause");
}